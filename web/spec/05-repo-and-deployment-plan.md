# Repository and deployment plan

This source lives in `web/` inside the existing aeDae repository. It does not deploy. A future
release requires separate human approval for hosting, DNS, redirects, mail, analytics, and security
headers.
