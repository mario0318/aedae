# Design system

Cool mineral ground, one grotesque system stack, monospace metrics, and keyed surface hues make the
system specific without decorative excess. The one shared glyph skeleton uses anchors `24/56/104`
and baseline `50`; each surface changes form to encode its role while retaining those anchors.
